# Next.js
.next/
out/

# Node
node_modules/

# Environment — NEVER commit these
.env
.env.local
.env.production

# public/data/subnets.json is intentionally TRACKED (not ignored).
# It is the canonical Phase 3/4 artifact committed by:
#   - GitHub Actions (.github/workflows/refresh-subnets.yml) on every refresh cycle
#   - Local: python scripts/fetch_subnets_subtensor.py  (then commit manually)
# Do NOT add it back to .gitignore — Vercel reads it from the repo at build time.

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db
